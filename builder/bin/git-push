#!/bin/sh
retry=$1
[ -z "$retry" ] && retry=10

for i in $(seq $retry)
do
	git pull --rebase
	git push origin master && success=1 || sleep $i
	[ -n "$success" ] && break
done

[ -z "$success" ] && exit 1
